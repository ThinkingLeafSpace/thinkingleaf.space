# 📋 删除分支操作指南

> 删除GitHub上不需要的分支

---

## ✅ 前提条件

**重要：** 在进行任何删除操作之前，请确保：
1. ✅ 已经在GitHub网页上将默认分支从 `delete` 切换到 `master`
2. ✅ 已经验证网站能正常显示最新内容
3. ✅ 没有重要的未同步代码在其他分支上

---

## 🗑️ 方法一：在GitHub网页删除（推荐）

### 为什么要用网页删除？
- ✅ 更安全，可以看到所有分支
- ✅ 可以快速切换默认分支
- ✅ 避免误删重要分支

### 操作步骤

#### 第一步：确认默认分支已切换
1. 访问：https://github.com/ThinkingLeafSpace/thinkingleaf.space
2. 查看仓库名称下方，确认显示 **`master`**（不是 `delete`）
3. 如果还是 `delete`，参考「🔧修复分支配置指南.md」先切换

#### 第二步：删除分支
1. 在仓库主页，点击 **"main"** 或分支下拉菜单旁的 **"View all branches"** 链接
2. 会看到所有分支列表，包括：
   - ✅ master（保留）
   - ❌ delete（删除）
   - ❌ main（删除，如果存在）
   - ❌ gh-pages（删除，如果存在）
   - ❌ ThinkingLeafSpace-V1.1（删除）
3. 找到要删除的分支，点击右侧的 **🗑️ 垃圾桶图标**
4. 确认删除

---

## 🔧 方法二：使用脚本删除

如果已经确认默认分支是master，可以使用命令行脚本：

```bash
cd "/Users/qianny/Nutstore Files/ThinkingLeafSpace 20250704com/ThinkingLeafSpace 20250924com"
bash 删除分支.sh
```

这个脚本会删除：
- `delete`
- `gh-pages`
- `main`
- `ThinkingLeafSpace-V1.1`

⚠️ **警告：** 脚本会询问确认，但请谨慎操作

---

## 📝 手动命令行删除（如果你熟悉Git）

如果你想精确控制删除哪些分支：

```bash
cd "/Users/qianny/Nutstore Files/ThinkingLeafSpace 20250704com/ThinkingLeafSpace 20250924com"

# 删除delete分支
git push origin --delete delete

# 删除main分支（如果存在）
git push origin --delete main

# 删除gh-pages分支（如果不需要）
git push origin --delete gh-pages

# 删除旧版本分支
git push origin --delete ThinkingLeafSpace-V1.1

# 清理本地引用
git remote prune origin
```

---

## ✅ 删除后验证

删除完成后，验证清理效果：

```bash
cd "/Users/qianny/Nutstore Files/ThinkingLeafSpace 20250704com/ThinkingLeafSpace 20250924com"

# 查看所有远程分支
git ls-remote --heads origin

# 应该只看到 master 分支
```

---

## 🎯 推荐操作顺序

1. **首先**：在GitHub网页切换默认分支到 master ✅
2. **然后**：验证网站显示正确内容 ✅
3. **最后**：删除不需要的分支

---

## 💡 什么时候删除gh-pages分支？

**保留gh-pages的情况：**
- 你想用 gh-pages 分支专门存放网站文件
- 你的部署流程依赖 gh-pages 分支

**删除gh-pages的情况：**
- 你的GitHub Pages直接从master部署
- 你想简化分支管理
- 当前没有使用gh-pages

---

## 🔍 检查你的GitHub Pages配置

如果不确定是否需要gh-pages分支：

1. 访问：https://github.com/ThinkingLeafSpace/thinkingleaf.space/settings/pages
2. 查看 **"Source"** 设置
   - 如果选择的是 `master` → 可以删除 `gh-pages`
   - 如果选择的是 `gh-pages` → 不能删除，否则网站会失效

**你的情况应该是直接从 master 部署，所以可以安全删除 gh-pages。**

---

删除完成后，你的仓库就会很干净了！只剩下有用的 master 分支。🎉

