# ⚠️ 紧急修复GitHub Pages 404问题

## 🔍 问题原因

删除分支后，GitHub Pages的配置可能被重置了。

## ✅ 快速修复步骤

### 方法1：在GitHub网页重新配置Pages（推荐）

#### 第一步：进入Pages设置
1. 访问：https://github.com/ThinkingLeafSpace/thinkingleaf.space/settings/pages
2. 如果没有自动跳转，进入：
   - 点击仓库右上角 **⚙️ Settings**
   - 左侧菜单找到 **"Pages"**

#### 第二步：配置Pages源
1. 在 **"Source"（源）** 部分：
   - **Branch（分支）**：选择 `master`
   - **Folder（文件夹）**：选择 `/ (root)`（根目录）
2. 点击 **"Save"（保存）** 按钮

#### 第三步：等待部署
1. 页面顶部会显示黄色或绿色的构建状态
2. 等待 **2-5分钟**，状态会变成绿色
3. 显示 "Your site is live at https://thinkingleaf.space"

#### 第四步：验证
1. 刷新网站：https://thinkingleaf.space
2. 应该可以看到内容了！

---

## 🔧 如果方法1不行，检查这些

### 检查1：确认默认分支是master
1. 访问：https://github.com/ThinkingLeafSpace/thinkingleaf.space
2. 查看仓库名称下方显示的分支
3. 如果不是`master`：
   - 点击分支名旁边的下拉箭头
   - 选择 `master`
   - 或者参考「设置master为主分支指南.md」

### 检查2：确认CNAME文件存在
```bash
cd "/Users/qianny/Nutstore Files/ThinkingLeafSpace 20250704com/ThinkingLeafSpace 20250924com"
ls -la CNAME
cat CNAME
```

应该显示：`thinkingleaf.space`

如果文件不存在或内容不对，重新创建：
```bash
echo "thinkingleaf.space" > CNAME
git add CNAME
git commit -m "恢复CNAME文件"
git push origin master
```

### 检查3：清除DNS缓存
如果网站已经部署但访问不了：

**macOS:**
```bash
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder
```

或者在浏览器中：
- **Chrome/Edge**: 打开开发者工具（F12） → 右键刷新按钮 → "清空缓存并硬性重新加载"
- **Safari**: Cmd+Option+E（清空缓存），然后 Cmd+R（刷新）

---

## 📝 完整操作流程（从零开始）

如果你想要一个完整的从头到尾的操作：

```bash
cd "/Users/qianny/Nutstore Files/ThinkingLeafSpace 20250704com/ThinkingLeafSpace 20250924com"

# 1. 确认当前分支
git branch --show-current  # 应该显示 master

# 2. 检查CNAME文件
cat CNAME  # 应该显示 thinkingleaf.space

# 3. 确认文件已推送
git log --oneline -1  # 确认最新提交

# 4. 推送最新内容（如果需要）
git push origin master

# 5. 然后在网页上配置Pages：
# https://github.com/ThinkingLeafSpace/thinkingleaf.space/settings/pages
# Source: master / (root)
```

---

## 🚨 如果还是404

### 检查自定义域名
1. 访问：https://github.com/ThinkingLeafSpace/thinkingleaf.space/settings/pages
2. 查看 **"Custom domain"** 部分
3. 确保显示：`thinkingleaf.space`
4. 如果没有，点击 **"Add domain"** 添加
5. 可能需要等待DNS生效（最长24小时，通常几分钟）

### 临时访问方法
在DNS更新前，可以先用GitHub默认域名访问：
```
https://thinkingleafspace.github.io/thinkingleaf.space
```

---

## ✅ 成功标志

配置成功后，你会看到：
1. ✅ GitHub Pages设置页面显示绿色对勾
2. ✅ 显示 "Your site is live at https://thinkingleaf.space"
3. ✅ 访问网站能看到内容（不是404）
4. ✅ 网站显示最新的文章和设计

---

## 📞 如果以上都不行

可能是GitHub的特殊情况，需要：
1. 等待更长时间（30分钟）
2. 清除浏览器缓存
3. 尝试无痕模式访问
4. 或者暂时回退到使用GitHub默认域名

---

**最重要的一步：在GitHub网页上重新配置Pages源为master/root！** 🎯

